# ── Core MCP ──────────────────────────────────────────────────────────────
mcp>=1.27.1

# ── HTTP provider: fetch + search ─────────────────────────────────────────
httpx>=0.28.1
ddgs>=9.14.4           # DuckDuckGo search (renamed from duckduckgo-search)

# ── Content extraction ────────────────────────────────────────────────────
beautifulsoup4>=4.14.3
trafilatura>=2.0.0    # Rich article extraction (optional, improves quality)

# ── Browser provider: automation/rendering ────────────────────────────────
playwright>=1.60.0
patchright>=1.60.0    # Stealth Chromium fork — Cloudflare/bot-detection bypass

# ── v3 app/ engine providers ──────────────────────────────────────────────
scrapling>=0.4.8
msgspec>=0.21.1       # Required by Scrapling dynamic/stealth fetchers on latest releases
pydantic>=2.13.4
curl_cffi>=0.15.0
browserforge>=1.2.4

# ── Optional: HTTP server mode ────────────────────────────────────────────
fastapi>=0.136.3
uvicorn>=0.48.0
websockets>=16.0

# ── Dev / testing ─────────────────────────────────────────────────────────
pytest>=9.0.3
pytest-asyncio>=1.4.0
pytest-timeout>=2.4.0
ruff>=0.15.14
